services:
  postgres:
    image: postgres
    container_name: estudo-python
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=123456
      - POSTGRES_DB=db_gerenciador_contas
    ports:
      - 5432:5432
    volumes:
      - ./postgres-data:/var/lib/postgresql/data